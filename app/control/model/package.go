// Package model contains the control-plane model registry, model
// specifications, and mode/capability enumerations.
//
// The package-level API mirrors Python's app.control.model package boundary:
// enums.go defines stable mode, tier, and capability values; spec.go defines
// model metadata helpers; and registry.go exposes model lookup and listing.
package model
